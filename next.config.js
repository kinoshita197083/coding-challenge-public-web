/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['images.ctfassets.net', 'images.ctfassets.net/', 'www.myzeller.com'],
    },
};

module.exports = nextConfig; 